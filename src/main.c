#include <stdio.h>
#include "proc.h"

int main(void) {
    printf("=== Process Monitor ===\n\n");

    // Stage 2 output:
    // list_processes();

    // Stage 3 output:
    //list_processes_with_names();

	// Stage 4 output:
	list_processes_with_stats();
    return 0;
}

